{
  "name": "qhda-monorepo",
  "private": true,
  "description": "Quantum-Hybrid DEX Accelerator",
  "workspaces": ["frontend"],
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "dev": "cd frontend && npm run dev"
  },
  "devDependencies": {},
  "engines": { "node": ">=18" }
}
